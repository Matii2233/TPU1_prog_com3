from funciones import get_couples

# Escribí una función recursiva combinaciones(lista, k) que reciba una lista de caracteres únicos,
# y un número k, e imprima todas las posibles cadenas de longitud k formadas con los caracteres dados 
# (permitiendo caracteres repetidos).

chars = ['a', 'b', 'c']
k = 2

get_couples(chars, k)